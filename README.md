# Система управления транспортной логистикой

Веб-приложение с многоуровневой архитектурой на основе стека PERN (PostgreSQL, Express, React, Node.js).

## Структура проекта

```
├── server/          # Backend (Node.js + Express + Sequelize)
│   ├── config/      # Конфигурация базы данных
│   ├── controllers/ # Контроллеры для обработки запросов
│   ├── models/      # Модели Sequelize
│   ├── routes/      # Маршруты API
│   └── index.js     # Точка входа сервера
├── client/          # Frontend (React)
│   ├── public/      # Статические файлы
│   └── src/         # Исходный код React
│       ├── components/ # React компоненты
│       └── services/  # API сервисы
└── package.json     # Корневой package.json
```

## Требования

- Node.js (версия 14 или выше)
- PostgreSQL (версия 12 или выше)
- npm или yarn

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd labs
```

### 2. Установка зависимостей

```bash
# Установка зависимостей для всего проекта
npm run install-all

# Или отдельно для сервера и клиента
npm run install-server
npm run install-client
```

### 3. Настройка базы данных PostgreSQL

Убедитесь, что PostgreSQL запущен на порту 5432. Создайте базу данных:

```sql
CREATE DATABASE transport_logistics;
```

### 4. Настройка переменных окружения

Создайте файл `server/.env` на основе `server/.env.example`:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=transport_logistics
DB_USER=postgres
DB_PASSWORD=postgres
PORT=5000

# JWT Configuration (обязательно для авторизации)
JWT_SECRET=your-secret-key-change-in-production-use-random-string
JWT_EXPIRES_IN=24h

# Frontend URL (для ссылок восстановления пароля)
FRONTEND_URL=http://localhost:3000

# SMTP Configuration (опционально, для восстановления пароля через email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Environment
NODE_ENV=development
```

**Важно:** 
- `JWT_SECRET` - обязательно измените на случайную строку в production
- SMTP настройки нужны только если планируете использовать восстановление пароля через email

### 5. Запуск приложения

```bash
# Запуск сервера и клиента одновременно
npm run dev

# Или отдельно:
npm run server  # Запуск только сервера на http://localhost:5000
npm run client  # Запуск только клиента на http://localhost:3000
```

При первом запуске сервер автоматически создаст таблицы в базе данных, включая таблицу пользователей.

### 6. Создание первого пользователя

После запуска приложения необходимо создать пользователя для входа в систему. Вы можете:

**Вариант 1:** Использовать форму регистрации в приложении:
1. Откройте `http://localhost:3000`
2. Перейдите на страницу регистрации
3. Заполните форму и создайте аккаунт

**Вариант 2:** Использовать API напрямую (через Postman):
```bash
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "email": "admin@test.com",
  "password": "password123",
  "firstName": "Администратор",
  "lastName": "Системы",
  "role": "admin"
}
```

**Роли пользователей:**
- `admin` - полный доступ ко всем функциям
- `manager` - может создавать, редактировать и удалять записи
- `user` - может только просматривать данные

**Примечание:** Для тестирования восстановления пароля без настройки SMTP, токен сброса будет возвращен в ответе API в режиме разработки (`NODE_ENV=development`).

## API Endpoints

### Авторизация и аутентификация (Auth)

**Важно:** Все эндпоинты ниже `/api/auth` требуют JWT токен в заголовке `Authorization: Bearer <token>`

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Авторизация пользователя (возвращает JWT токен)
- `GET /api/auth/me` - Получение информации о текущем пользователе (требует токен)
- `PUT /api/auth/change-password` - Смена пароля (требует токен)
- `POST /api/auth/forgot-password` - Запрос на восстановление пароля
- `POST /api/auth/reset-password` - Сброс пароля по токену

**Пример регистрации:**
```json
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123",
  "firstName": "Иван",
  "lastName": "Иванов",
  "role": "user"
}
```

**Пример авторизации:**
```json
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**Использование токена:**
После успешной авторизации добавьте заголовок ко всем запросам:
```
Authorization: Bearer <ваш_jwt_токен>
```

### Транспортные средства (Vehicles)

**Требует аутентификации:** Да (JWT токен)
**Создание/обновление/удаление:** Только для ролей `admin` и `manager`

- `POST /api/vehicles` - Создание нового транспортного средства (требует роль admin/manager)
- `GET /api/vehicles` - Получение списка с пагинацией, сортировкой, фильтрацией и поиском (доступно всем авторизованным)
- `GET /api/vehicles/:id` - Получение транспортного средства по ID (доступно всем авторизованным)
- `PUT /api/vehicles/:id` - Обновление транспортного средства (требует роль admin/manager)
- `DELETE /api/vehicles/:id` - Удаление транспортного средства (требует роль admin/manager)
- `GET /api/vehicles/:id/exists` - Проверка существования транспортного средства (доступно всем авторизованным)

**Параметры запроса для GET /api/vehicles:**
- `page` - номер страницы (по умолчанию: 1)
- `limit` - количество записей на странице (по умолчанию: 10)
- `sortBy` - поле для сортировки (id, licensePlate, brand, model, vehicleType, capacity, status, year)
- `sortOrder` - порядок сортировки (ASC или DESC)
- `search` - поиск по номерному знаку, марке, модели
- `status` - фильтр по статусу (available, in_use, maintenance, retired)
- `vehicleType` - фильтр по типу (truck, van, car, motorcycle)
- `minCapacity` - минимальная грузоподъемность
- `maxCapacity` - максимальная грузоподъемность
- `minYear` - минимальный год выпуска
- `maxYear` - максимальный год выпуска

### Маршруты (Routes)

**Требует аутентификации:** Да (JWT токен)
**Создание/обновление/удаление:** Только для ролей `admin` и `manager`

- `POST /api/routes` - Создание нового маршрута
- `GET /api/routes` - Получение списка с пагинацией, сортировкой, фильтрацией и поиском
- `GET /api/routes/:id` - Получение маршрута по ID
- `PUT /api/routes/:id` - Обновление маршрута
- `DELETE /api/routes/:id` - Удаление маршрута
- `GET /api/routes/:id/exists` - Проверка существования маршрута

**Параметры запроса для GET /api/routes:**
- `page`, `limit`, `sortBy`, `sortOrder` - аналогично Vehicles
- `search` - поиск по названию, точке отправления, точке назначения
- `status` - фильтр по статусу (active, inactive, archived)
- `origin` - фильтр по точке отправления
- `destination` - фильтр по точке назначения
- `minDistance` - минимальное расстояние
- `maxDistance` - максимальное расстояние
- `minTime` - минимальное время в минутах
- `maxTime` - максимальное время в минутах

### Грузоперевозки (Shipments)

**Требует аутентификации:** Да (JWT токен)
**Создание/обновление/удаление:** Только для ролей `admin` и `manager`

- `POST /api/shipments` - Создание новой грузоперевозки
- `GET /api/shipments` - Получение списка с пагинацией, сортировкой, фильтрацией и поиском
- `GET /api/shipments/:id` - Получение грузоперевозки по ID
- `PUT /api/shipments/:id` - Обновление грузоперевозки
- `DELETE /api/shipments/:id` - Удаление грузоперевозки
- `GET /api/shipments/:id/exists` - Проверка существования грузоперевозки

**Параметры запроса для GET /api/shipments:**
- `page`, `limit`, `sortBy`, `sortOrder` - аналогично Vehicles
- `search` - поиск по описанию груза
- `status` - фильтр по статусу (pending, in_transit, delivered, cancelled)
- `vehicleId` - фильтр по ID транспортного средства
- `routeId` - фильтр по ID маршрута
- `minWeight` - минимальный вес
- `maxWeight` - максимальный вес
- `departureDateFrom` - дата отправления от (формат: YYYY-MM-DD)
- `departureDateTo` - дата отправления до (формат: YYYY-MM-DD)

### Экспорт данных (Export)

**Требует аутентификации:** Да (JWT токен)

- `GET /api/export/shipments/excel` - Экспорт отчета по грузоперевозкам в Excel (xlsx)
- `GET /api/export/shipments/pdf` - Экспорт отчета по грузоперевозкам в PDF
- `GET /api/export/vehicles/excel` - Экспорт сводки по транспортным средствам в Excel (xlsx)
- `GET /api/export/vehicles/pdf` - Экспорт сводки по транспортным средствам в PDF

**Параметры запроса для экспорта грузоперевозок:**
- `startDate` - дата начала периода (опционально, формат: YYYY-MM-DD)
- `endDate` - дата окончания периода (опционально, формат: YYYY-MM-DD)

**Примеры запросов:**

Экспорт грузоперевозок за период в Excel:
```
GET http://localhost:5000/api/export/shipments/excel?startDate=2024-01-01&endDate=2024-01-31
Authorization: Bearer <your_jwt_token>
```

Экспорт сводки по транспортным средствам в PDF:
```
GET http://localhost:5000/api/export/vehicles/pdf
Authorization: Bearer <your_jwt_token>
```

### Аналитика (Analytics)

**Требует аутентификации:** Да (JWT токен)

- `GET /api/analytics/shipments-by-month` - Статистика грузоперевозок по месяцам
- `GET /api/analytics/shipments-by-status` - Распределение грузоперевозок по статусам
- `GET /api/analytics/top-routes` - Топ маршрутов по количеству перевозок
- `GET /api/analytics/vehicles-by-type` - Распределение транспортных средств по типам
- `GET /api/analytics/dashboard-stats` - Общая статистика для дашборда

**Параметры запроса:**
- `startDate` - дата начала периода (для shipments-by-month, формат: YYYY-MM-DD)
- `endDate` - дата окончания периода (для shipments-by-month, формат: YYYY-MM-DD)
- `limit` - количество записей для top-routes (по умолчанию: 10)

**Дашборд аналитики:**
- Доступен по адресу `/dashboard` после авторизации
- Содержит 4 типа графиков:
  1. **Линейный график** - динамика грузоперевозок по месяцам (количество и вес)
  2. **Круговая диаграмма** - распределение грузоперевозок по статусам
  3. **Столбчатая диаграмма** - топ-10 маршрутов по количеству перевозок
  4. **Столбчатая диаграмма** - распределение транспортных средств по типам
- Фильтрация по периоду для линейного графика
- Интерактивные подсказки (tooltips) при наведении на элементы графиков
- Все подписи и заголовки на русском языке

**Формат отчетов:**

1. **Отчет по грузоперевозкам:**
   - Заголовок с названием отчета и периодом
   - Таблица с данными о грузоперевозках (ID, транспорт, маршрут, описание груза, вес, статус, даты)
   - Итоговая строка с общим весом
   - Статистика по статусам

2. **Сводка по транспортным средствам:**
   - Заголовок с названием отчета и датой формирования
   - Группировка по типам транспортных средств (грузовики, фургоны, автомобили, мотоциклы)
   - Таблица с данными о каждом транспортном средстве
   - Итоговая статистика (общее количество, общая грузоподъемность)
   - Статистика по статусам

## Модели базы данных

### Vehicle (Транспортные средства)
- `id` - уникальный идентификатор
- `licensePlate` - номерной знак (уникальный)
- `brand` - марка
- `model` - модель
- `vehicleType` - тип (truck, van, car, motorcycle)
- `capacity` - грузоподъемность (тонны)
- `status` - статус (available, in_use, maintenance, retired)
- `year` - год выпуска
- `createdAt`, `updatedAt` - временные метки

### Route (Маршруты)
- `id` - уникальный идентификатор
- `name` - название маршрута
- `origin` - точка отправления
- `destination` - точка назначения
- `distance` - расстояние (км)
- `estimatedTime` - оценочное время (минуты)
- `status` - статус (active, inactive, archived)
- `createdAt`, `updatedAt` - временные метки

### Shipment (Грузоперевозки)
- `id` - уникальный идентификатор
- `vehicleId` - ID транспортного средства (внешний ключ)
- `routeId` - ID маршрута (внешний ключ)
- `cargoDescription` - описание груза
- `weight` - вес (тонны)
- `status` - статус (pending, in_transit, delivered, cancelled)
- `departureDate` - дата отправления
- `deliveryDate` - дата доставки
- `createdAt`, `updatedAt` - временные метки

## Валидация данных

Все модели используют встроенные и пользовательские валидаторы Sequelize:
- Проверка обязательных полей
- Проверка типов данных
- Проверка диапазонов значений
- Проверка уникальности
- Пользовательские валидаторы для бизнес-логики

## Тестирование API

Для тестирования API можно использовать Postman. Импортируйте коллекцию из файла `postman_collection.json` или создайте запросы вручную.

### Тестовые сценарии для Postman

#### 1. Транспортные средства (Vehicles)

##### Сценарий 1.1: Создание нового транспортного средства
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/vehicles`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "licensePlate": "А123БВ777",
  "brand": "Mercedes",
  "model": "Actros",
  "vehicleType": "truck",
  "capacity": 20.5,
  "status": "available",
  "year": 2020,
  "photoUrl": "https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=500"
}
```
- **Ожидаемый результат:** HTTP 201, возвращает созданный объект с полями `id`, `createdAt`, `updatedAt`

##### Сценарий 1.2: Получение списка транспортных средств (базовый запрос)
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles`
- **Ожидаемый результат:** HTTP 200, возвращает объект с полями `data` (массив) и `pagination` (объект с `total`, `page`, `limit`, `totalPages`)

##### Сценарий 1.3: Получение списка с пагинацией
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles?page=2&limit=5`
- **Ожидаемый результат:** HTTP 200, возвращает 5 записей на второй странице

##### Сценарий 1.4: Получение списка с сортировкой
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles?sortBy=year&sortOrder=DESC`
- **Ожидаемый результат:** HTTP 200, список отсортирован по году выпуска по убыванию

##### Сценарий 1.5: Фильтрация по статусу и типу
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles?status=available&vehicleType=truck`
- **Ожидаемый результат:** HTTP 200, возвращает только доступные грузовики

##### Сценарий 1.6: Поиск по нескольким полям
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles?search=Mercedes`
- **Ожидаемый результат:** HTTP 200, возвращает записи, содержащие "Mercedes" в номере, марке или модели

##### Сценарий 1.7: Фильтрация по диапазону грузоподъемности
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles?minCapacity=10&maxCapacity=25`
- **Ожидаемый результат:** HTTP 200, возвращает транспортные средства с грузоподъемностью от 10 до 25 тонн

##### Сценарий 1.8: Получение транспортного средства по ID
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles/1`
- **Ожидаемый результат:** HTTP 200, возвращает объект транспортного средства с указанным ID

##### Сценарий 1.9: Получение несуществующего транспортного средства
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles/99999`
- **Ожидаемый результат:** HTTP 404, сообщение об ошибке "Транспортное средство не найдено"

##### Сценарий 1.10: Обновление транспортного средства
- **Метод:** `PUT`
- **URL:** `http://localhost:5000/api/vehicles/1`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "status": "in_use"
}
```
- **Ожидаемый результат:** HTTP 200, возвращает обновленный объект

##### Сценарий 1.11: Удаление транспортного средства
- **Метод:** `DELETE`
- **URL:** `http://localhost:5000/api/vehicles/1`
- **Ожидаемый результат:** HTTP 200, сообщение об успешном удалении

##### Сценарий 1.12: Проверка существования транспортного средства
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles/1/exists`
- **Ожидаемый результат:** HTTP 200, возвращает `{"exists": true}` или `{"exists": false}`

##### Сценарий 1.13: Создание с невалидными данными (отрицательная грузоподъемность)
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/vehicles`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "licensePlate": "ТЕСТ123",
  "brand": "Test",
  "model": "Test",
  "vehicleType": "truck",
  "capacity": -5,
  "status": "available",
  "year": 2020
}
```
- **Ожидаемый результат:** HTTP 400, сообщение об ошибке валидации

##### Сценарий 1.14: Создание с дублирующимся номерным знаком
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/vehicles`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):** Используйте существующий `licensePlate`
- **Ожидаемый результат:** HTTP 400, сообщение о нарушении уникальности

#### 2. Маршруты (Routes)

##### Сценарий 2.1: Создание нового маршрута
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/routes`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "name": "Москва - Санкт-Петербург",
  "origin": "Москва",
  "destination": "Санкт-Петербург",
  "distance": 700.5,
  "estimatedTime": 480,
  "status": "active",
  "photoUrl": "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=500"
}
```
- **Ожидаемый результат:** HTTP 201, возвращает созданный маршрут

##### Сценарий 2.2: Получение списка маршрутов с фильтрацией по статусу
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/routes?status=active`
- **Ожидаемый результат:** HTTP 200, возвращает только активные маршруты

##### Сценарий 2.3: Поиск маршрутов по названию и городам
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/routes?search=Москва`
- **Ожидаемый результат:** HTTP 200, возвращает маршруты, содержащие "Москва" в названии, точке отправления или назначения

##### Сценарий 2.4: Фильтрация по диапазону расстояния
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/routes?minDistance=100&maxDistance=1000`
- **Ожидаемый результат:** HTTP 200, возвращает маршруты с расстоянием от 100 до 1000 км

##### Сценарий 2.5: Получение маршрута по ID
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/routes/1`
- **Ожидаемый результат:** HTTP 200, возвращает объект маршрута

##### Сценарий 2.6: Обновление маршрута
- **Метод:** `PUT`
- **URL:** `http://localhost:5000/api/routes/1`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "status": "inactive"
}
```
- **Ожидаемый результат:** HTTP 200, возвращает обновленный маршрут

##### Сценарий 2.7: Удаление маршрута
- **Метод:** `DELETE`
- **URL:** `http://localhost:5000/api/routes/1`
- **Ожидаемый результат:** HTTP 200, сообщение об успешном удалении

##### Сценарий 2.8: Проверка существования маршрута
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/routes/1/exists`
- **Ожидаемый результат:** HTTP 200, возвращает `{"exists": true}`

#### 3. Грузоперевозки (Shipments)

##### Сценарий 3.1: Создание новой грузоперевозки
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/shipments`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "vehicleId": 1,
  "routeId": 1,
  "cargoDescription": "Электроника и бытовая техника",
  "weight": 15.5,
  "status": "pending",
  "departureDate": "2024-01-15T10:00:00Z",
  "deliveryDate": "2024-01-16T18:00:00Z",
  "photoUrl": "https://images.unsplash.com/photo-1586864387967-d02ef85d93e8?w=500"
}
```
- **Ожидаемый результат:** HTTP 201, возвращает созданную грузоперевозку с связанными объектами `vehicle` и `route`

##### Сценарий 3.2: Получение списка грузоперевозок с фильтрацией по статусу
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/shipments?status=pending`
- **Ожидаемый результат:** HTTP 200, возвращает только ожидающие грузоперевозки

##### Сценарий 3.3: Фильтрация по транспортному средству и маршруту
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/shipments?vehicleId=1&routeId=1`
- **Ожидаемый результат:** HTTP 200, возвращает грузоперевозки для указанного транспорта и маршрута

##### Сценарий 3.4: Поиск по описанию груза
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/shipments?search=электроника`
- **Ожидаемый результат:** HTTP 200, возвращает грузоперевозки с указанным текстом в описании

##### Сценарий 3.5: Фильтрация по диапазону веса
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/shipments?minWeight=10&maxWeight=20`
- **Ожидаемый результат:** HTTP 200, возвращает грузоперевозки с весом от 10 до 20 тонн

##### Сценарий 3.6: Фильтрация по дате отправления
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/shipments?departureDateFrom=2024-01-01&departureDateTo=2024-01-31`
- **Ожидаемый результат:** HTTP 200, возвращает грузоперевозки с датой отправления в указанном диапазоне

##### Сценарий 3.7: Получение грузоперевозки по ID
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/shipments/1`
- **Ожидаемый результат:** HTTP 200, возвращает объект грузоперевозки с связанными `vehicle` и `route`

##### Сценарий 3.8: Обновление статуса грузоперевозки
- **Метод:** `PUT`
- **URL:** `http://localhost:5000/api/shipments/1`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "status": "in_transit"
}
```
- **Ожидаемый результат:** HTTP 200, возвращает обновленную грузоперевозку

##### Сценарий 3.9: Удаление грузоперевозки
- **Метод:** `DELETE`
- **URL:** `http://localhost:5000/api/shipments/1`
- **Ожидаемый результат:** HTTP 200, сообщение об успешном удалении

##### Сценарий 3.10: Создание грузоперевозки с несуществующим транспортным средством
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/shipments`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "vehicleId": 99999,
  "routeId": 1,
  "cargoDescription": "Тест",
  "weight": 10,
  "status": "pending",
  "departureDate": "2024-01-15T10:00:00Z"
}
```
- **Ожидаемый результат:** HTTP 404, сообщение "Транспортное средство не найдено"

##### Сценарий 3.11: Создание с датой доставки раньше даты отправления
- **Метод:** `POST`
- **URL:** `http://localhost:5000/api/shipments`
- **Headers:** `Content-Type: application/json`
- **Body (JSON):**
```json
{
  "vehicleId": 1,
  "routeId": 1,
  "cargoDescription": "Тест",
  "weight": 10,
  "status": "pending",
  "departureDate": "2024-01-15T10:00:00Z",
  "deliveryDate": "2024-01-14T10:00:00Z"
}
```
- **Ожидаемый результат:** HTTP 400, сообщение об ошибке валидации

#### 4. Комбинированные сценарии

##### Сценарий 4.1: Комплексная фильтрация и сортировка
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/vehicles?status=available&vehicleType=truck&minCapacity=15&maxCapacity=25&sortBy=year&sortOrder=DESC&page=1&limit=10`
- **Ожидаемый результат:** HTTP 200, возвращает доступные грузовики с грузоподъемностью 15-25 тонн, отсортированные по году выпуска по убыванию

##### Сценарий 4.2: Health Check
- **Метод:** `GET`
- **URL:** `http://localhost:5000/api/health`
- **Ожидаемый результат:** HTTP 200, возвращает `{"status": "OK", "message": "Server is running"}`

### Порядок выполнения тестов

Рекомендуемый порядок выполнения тестовых сценариев:

1. **Подготовка данных:**
   - Создайте несколько транспортных средств (сценарии 1.1)
   - Создайте несколько маршрутов (сценарии 2.1)

2. **Тестирование CRUD операций:**
   - Выполните все операции CRUD для каждой сущности
   - Проверьте обработку ошибок (несуществующие ID, валидация)

3. **Тестирование фильтрации и поиска:**
   - Протестируйте различные комбинации фильтров
   - Проверьте пагинацию и сортировку

4. **Тестирование связей:**
   - Создайте грузоперевозки с существующими транспортными средствами и маршрутами
   - Проверьте получение связанных данных

5. **Тестирование граничных случаев:**
   - Попробуйте создать записи с невалидными данными
   - Проверьте удаление записей, используемых в связях

## Особенности реализации

1. **CRUD операции** - полный набор операций для всех сущностей
2. **Пагинация** - поддержка разбиения результатов на страницы
3. **Сортировка** - сортировка по любому полю в порядке возрастания или убывания
4. **Фильтрация** - фильтрация по одному или нескольким полям одновременно
5. **Поиск** - поиск по нескольким полям одновременно с использованием LIKE запросов
6. **Валидация** - валидация на уровне моделей с понятными сообщениями об ошибках
7. **Обработка ошибок** - корректная обработка случаев отсутствия записей и других ошибок
8. **Связи между моделями** - использование внешних ключей и связей Sequelize

## История коммитов

Проект ведется с использованием конвенции коммитов:
- `init` - инициализация проекта
- `feat` - новая функциональность
- `refactor` - рефакторинг кода
- `fix` - исправление ошибок
- `docs` - обновление документации
